hocus 0.1;

/* A block comment with fake syntax: node ignored: "null" {} */
graph highlighting {
  target "/obj/geo1";
  category Sop;
  node source @id("asset.source-01"): "attribwrangle" {
    snippet = vex`string label = "escaped \` backtick";`;
  }
}
